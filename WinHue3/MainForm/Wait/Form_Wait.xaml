<Window x:Class="WinHue3.MainForm.Wait.Form_Wait"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resx="clr-namespace:WinHue3.Resources"
        xmlns:wait="clr-namespace:WinHue3.MainForm.Wait"
        mc:Ignorable="d"
        Title="{x:Static resx:GUI.WaitForm_Title}" Height="104.548" Width="367.855" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True" WindowStyle="None" WindowStartupLocation="CenterOwner" BorderBrush="#FF4B4B4B">
    <Window.DataContext>
        <wait:WaitViewModel/>
    </Window.DataContext>
    <Grid OverridesDefaultStyle="True" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <ProgressBar x:Name="PbWait" Maximum="{Binding WaitTime}" Value="{Binding pbValue}" Grid.Row="1"/>
        <TextBlock x:Name="TblMessage" TextWrapping="Wrap" Text="{Binding Message}"/>

    </Grid>
</Window>
