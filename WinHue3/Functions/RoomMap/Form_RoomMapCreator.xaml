<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinHue3.Functions.RoomMap"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:l="clr-namespace:WinHue3.Philips_Hue.HueObjects.LightObject"
        xmlns:behaviors="clr-namespace:WinHue3.Functions.Behaviors"
        xmlns:dd="urn:gong-wpf-dragdrop"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        x:Class="WinHue3.Functions.RoomMap.Form_RoomMapCreator"
        mc:Ignorable="d" x:Name="RoomMapWindow"
        Title="Form_RoomMap" Height="675.313" Width="1029.333" WindowStartupLocation="CenterOwner" MinHeight="768" MinWidth="1024">
    <Window.DataContext>
        <local:RoomMapViewModel/>
    </Window.DataContext>
    <Grid>
        <ListBox HorizontalAlignment="Left" Width="196" Margin="0,45,0,204.667" ItemsSource="{Binding ListLights}" dd:DragDrop.IsDragSource="True" dd:DragDrop.UseDefaultDragAdorner="True" dd:DragDrop.DropHandler="{Binding}" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <ListBox.Resources>
                <DataTemplate DataType="{x:Type l:Light}">
                    <WrapPanel >
                        <Image Height="32" Source="{Binding Image}"/>
                        <Label Content="{Binding name}" />
                    </WrapPanel>
                </DataTemplate>
            </ListBox.Resources>
        </ListBox>
        <ListBox x:Name="DragDropMap" Margin="196,45,-0.333,39.667" SelectedItem="{Binding SelectedItem}" ItemsSource="{Binding ListCanvasLights}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Canvas.Top" Value="{Binding Y}"></Setter>
                    <Setter Property="Canvas.Left" Value="{Binding X}"></Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Width="{Binding ActualWidth, ElementName=DragDropMap}" Height="{Binding ActualHeight, ElementName=DragDropMap}" dd:DragDrop.IsDropTarget="True"  dd:DragDrop.DropHandler="{Binding}" dd:DragDrop.UseDefaultDragAdorner="True" IsItemsHost="True" >
                        <Canvas.Background>
                            <ImageBrush ImageSource="{Binding FloorPlan}" Stretch="None"/>
                        </Canvas.Background>
                    </Canvas>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="local:HueElement">
                    <WrapPanel Height="{Binding PanelHeight}" Width="{Binding PanelWidth}" Background="{x:Null}" ToolTip="{Binding name}">
                        <i:Interaction.Behaviors>
                            <behaviors:DragBehavior/>
                        </i:Interaction.Behaviors>
                        <Image Source="/WinHue3;component/Resources/HueLightOn-Large.png" Height="{Binding ImageHeight}" Width="{Binding ImageWidth}"/>
                        <TextBlock Text="{Binding Label}" Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=WrapPanel}, Path=ActualWidth  }" TextAlignment="Center" TextWrapping="Wrap" Visibility="{Binding LabelVisible}"/>
                    </WrapPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ToolBarTray Margin="196,0,-0.333,0" Height="45" IsLocked="True" Background="{StaticResource HeaderBackgroundBrush}" VerticalAlignment="Top">
            <ToolBar Header="Files">
                <Button>
                    <Image Source="/WinHue3;component/Resources/save.png" Height="32" Width="32"/>
                </Button>
                <Button>
                    <Image Height="32" Width="32" Source="/WinHue3;component/Resources/close_delete.png"></Image>
                </Button>
            </ToolBar>
            <ToolBar Height="43" Header="Floor plan">
                <Button Height="32" Width="32" Command="{Binding ChooseImageCommand}">
                    <Image Source="/WinHue3;component/Resources/floorplan.png"></Image>
                </Button>
            </ToolBar>
            
        </ToolBarTray>
        <ListBox HorizontalAlignment="Left" Margin="0,525,0,39.667" Width="196" ItemsSource="{Binding ListFloors}" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="local:Floor">
                    <Label Content="{Binding Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Done" HorizontalAlignment="Left" Margin="938,709,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</Window>

